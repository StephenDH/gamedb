<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :genre_id %><br>
    <%= f.number_field :genre_id %>
  </div>
  <div class="field">
    <%= f.label :discription %><br>
    <%= f.text_area :discription, size: "60x12" %>
  </div>
  <div class="field">
    <%= f.label :release %><br>
    <%= f.number_field :release %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
